// Properties DOM manipulation --> we use propertices to navigate

firstCHild = document.querySelector("div").children;
console.dir(firstCHild);

// Three type of notes are have in DOM : 
// 1. text note, 2. comment note, 3. element note

innerText = document.querySelector("p").innerText;
console.dir(innerText);